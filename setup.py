from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Python tool to segment ECS images and construct an MCell4 model geometry of the ECS space. ',
    author='Blake Wilson',
    license='MIT',
)
